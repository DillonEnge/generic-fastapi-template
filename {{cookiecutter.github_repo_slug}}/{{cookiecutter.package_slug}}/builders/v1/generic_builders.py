from typing import Dict


# This only serves as a workflow example for more complex builds in the future
def build_resource(payload: Dict):
    """Build a generic resource.

    Args:
        payload: The payload to build a resource from.

    Returns:
        The newly built resource
    """
    return payload
